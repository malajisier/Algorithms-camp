#include <iostream>
#include <vector>

using namespace std;

int main() {
    vector<vector<int>> a {{0, 1, 2, 3, 4}, {2, 4, 4, 4, 5}};
    // int s = 0;
    // for (auto x : a)
    //     s += x;
    // cout << s ;
    cout << a.size();
}